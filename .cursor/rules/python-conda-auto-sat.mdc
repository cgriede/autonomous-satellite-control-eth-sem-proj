---
description: Use auto-sat conda environment for Python commands
alwaysApply: true
---

# Python Environment

- Always run Python commands in this project after activating the `auto-sat` conda environment.
- Use: `conda activate auto-sat` before `python ...`, `pip ...`, pytest, or any Python tooling command.
